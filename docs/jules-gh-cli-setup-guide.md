# Jules + GitHub CLI setup guide

Public fallback from connector (gist creation not exposed by current connector).

Use GH_TOKEN in Jules, install gh during setup, validate with:

```bash
gh repo view d-o-hub/do-gist-hub
```