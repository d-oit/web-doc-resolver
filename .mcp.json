{
  "mcpServers": {
    "web-doc-resolver": {
      "command": "python",
      "args": ["scripts/resolve.py"],
      "env": {
        "EXA_API_KEY": "${EXA_API_KEY}",
        "TAVILY_API_KEY": "${TAVILY_API_KEY}",
        "FIRECRAWL_API_KEY": "${FIRECRAWL_API_KEY}"
      }
    }
  }
}
